[build]
builder = "dockerfile"
dockerfilePath = "./Dockerfile"
watchPatterns = ["functions/**/*.ts", "Dockerfile"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
